name: Import Bitcoin Addresses from Artifact to SQLite & Upload to R2

on:
  workflow_dispatch:

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (AWS CLI is pre-installed)
        run: |
          sudo apt-get update
          sudo apt-get install -y g++ sqlite3 libsqlite3-dev unzip gzip

      - name: Download artifact from GitHub
        run: |
          curl -L -H "Authorization: token ${{ secrets.GH_PAT }}" \
            "https://api.github.com/repos/sinescode/bitadressa/actions/artifacts/5907316265/zip" \
            -o artifact.zip
          if [ $(stat -c%s artifact.zip) -lt 1000000 ]; then
            echo "Downloaded file is too small – check your token or artifact ID"
            exit 1
          fi

      - name: Extract artifact.zip
        run: |
          mkdir step1
          unzip -j artifact.zip -d step1
          echo "Contents of step1:"
          ls -la step1

      - name: Locate .gz file (either directly or inside another zip)
        run: |
          # If a .gz file exists directly, use it
          if ls step1/*.gz 1> /dev/null 2>&1; then
            echo "Found .gz directly"
            mv step1/*.gz input.tsv.gz
          else
            # Otherwise look for a .zip and extract further
            ZIP_FILE=$(ls step1/*.zip | head -n1)
            if [ -z "$ZIP_FILE" ]; then
              echo "No .gz or .zip found in step1"
              exit 1
            fi
            echo "Found inner zip: $ZIP_FILE"
            mkdir step2
            unzip -j "$ZIP_FILE" -d step2
            echo "Contents of step2:"
            ls -la step2
            if ! ls step2/*.gz 1> /dev/null 2>&1; then
              echo "No .gz found inside inner zip"
              exit 1
            fi
            mv step2/*.gz input.tsv.gz
          fi

      - name: Decompress .gz → .tsv
        run: gunzip -v input.tsv.gz

      - name: Compile importer
        run: g++ -O2 -std=c++20 tsv2sqlite.cpp -lsqlite3 -o tsv_to_sqlite

      - name: Run importer
        run: ./tsv_to_sqlite input.tsv addresses.db

      - name: Configure AWS CLI for R2
        run: |
          aws configure set aws_access_key_id ${{ secrets.R2_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.R2_SECRET_ACCESS_KEY }}
          aws configure set region auto

      - name: Upload database to R2 with timestamp
        run: |
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          aws s3 --endpoint-url ${{ secrets.R2_ENDPOINT }} \
            cp addresses.db s3://sqlitedatabase/addresses_${TIMESTAMP}.db